# shellcheck shell=bash
#
# Personal dev-shell customizations.
#
# linux-setup creates this file once and never overwrites it. It is sourced
# after every managed dev-shell file, so aliases, functions, exports, and prompt
# changes here can extend or override the managed defaults.
#
# Examples:
# export EDITOR=vim
# alias ll='ls -alF'
